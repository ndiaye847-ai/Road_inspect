# Android / Gradle build outputs
*.iml
.gradle/
/build
/app/build
/captures
.externalNativeBuild/
.cxx/

# Local environment (machine-specific, never commit)
local.properties

# Keystores and signing material — never commit real keys
*.jks
*.keystore
keystore/

# IDE
.idea/
*.iml
